<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MiMusic</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="shortcut icon" href="uploads/images/peque-removebg-preview.png" type="image/x-icon">
</head>

<body>

    <div class="header-container">
        <img src="uploads/images/peque-removebg-preview.png" alt="MiMusic Logo" class="logo">
        <h1 class="animated-heading">Mi<span class="music">Music</span></h1>
    </div>

    <div class="login-register-buttons">
        <a href="/login"><button id="login-button">Login</button></a>
        <a href="/register"><button id="register-button">Register</button></a>
    </div>

    <script src="https://kit.fontawesome.com/ef9a692198.js" crossorigin="anonymous"></script>
</body>
<div class="background-image"></div>
<style>
    body {
        font-family: 'Montserrat', sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        margin: 0;
        box-sizing: border-box;
    }

    html,
    body {
        margin: 0;
        padding: 0;
    }

    .header-container {
        display: flex;
        align-items: center;
        margin-bottom: 20px;
    }

 .logo {
    width: 88px;
    height: auto;
    margin-right: 10px;
}

    .animated-heading {
        display: flex;
        align-items: center;
        color: #ffffff;
        opacity: 0;
        transform: translateY(-20px);
        animation: fadeInUp 1s ease-out forwards;
        font-size: 88px;
    }

    .animated-heading h1 {
        margin: 0; 
    }

    .music {
        color: #DB5B1C;
        margin-left: 5px; 
    }

    .login-register-buttons {
        display: flex;
        gap: 20px;
    }

    button {
        background-color: #ffffff;
        color: #000000;
        font-size: 16px;
        font-weight: bold;
        padding: 14px 30px;
        border: 0px;
        border-radius: 40px;
        cursor: pointer;
    }

    #login-button:hover,
    #register-button:hover {
        background-color: #DB5B1C; 
        color: #ffffff; 
    }

    .background-image {
        background: url('uploads/images/fondo.png') center/cover fixed;
        filter: blur(10px);
        width: 100%;
        height: 100%;
        position: fixed;
        z-index: -1;
    }

    @keyframes fadeInUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>

</html>
